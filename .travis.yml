sudo: required
services:
  - docker

before_install:
  - docker build -t makesaa/docker-react -f Dockerfile.dev .

 script:
    - docker run -e CI=true makesaa/docker-react npm run test -- --coverage
